inspecode:
  fortify:
    options:
      --build-cmd:
        - ./bootstrap
        - env CC="sourceanalyzer -b build_id $@ gcc" CXX="sourceanalyzer -b build_id $@ g++" ./configure -encoding -D -exclude
        - make
