inspecode:
  coverity:
    options:
      --commands:
        - sudo apt-get update && sudo apt-get install -y ntp ntpdate
        - ./bootstrap
        - ./configure
        - cov-build --dir ~/cov-im $@ make
